{
  "name": "@visioncore/native-shared",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "VisionCore cliente nativo — shared-core (TS) + skeleton Tauri (NO compilado en este entorno).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
